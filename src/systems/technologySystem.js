/* ============================================================================
 * technologySystem.js — 【骨架，尚未實作】
 *
 * 規劃職責：
 *   - refreshTechnologyStatuses(playerState)：依學院等級解鎖的 tier 與已完成
 *     科技，把符合條件的科技狀態更新為 'available'。
 *   - canQueueResearch／queueResearch／resolveResearch：研究佇列（同時只能
 *     研究一項，規則由此系統強制，非 TechnologyState 資料形狀本身限制）。
 *
 * 對應舊版 js/research.js。
 * ==========================================================================*/

(function () {
  window.Game.Systems.Technology = {
    // refreshTechnologyStatuses(playerState) { ... }
    // queueResearch(playerState, technologyDefId, now) { ... }
    // resolveResearch(playerState, now) { ... }
  };
})();
