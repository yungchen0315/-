/* ============================================================================
 * achievementSystem.js — 【骨架，尚未實作】
 *
 * 規劃職責：
 *   - checkAchievements(saveGame, playerState)：依 ACHIEVEMENT_DEFS 的宣告式
 *     condition（見 src/data/achievementDefs.js）逐一解讀並判斷是否達成，
 *     這裡是唯一知道「condition.type 該怎麼解讀」的地方，資料層本身不含邏輯。
 *
 * 對應舊版 js/achievements.js。
 * ==========================================================================*/

(function () {
  window.Game.Systems.Achievement = {
    // checkAchievements(saveGame, playerState) { ... }
  };
})();
