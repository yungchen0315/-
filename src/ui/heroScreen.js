/* ============================================================================
 * heroScreen.js — 【骨架，尚未實作】「武將」分頁畫面（名錄、探索、裝備）。
 * ==========================================================================*/

(function () {
  window.Game.UI.HeroScreen = {
    // render(container, saveGame, playerState) { ... }
  };
})();
