/* ============================================================================
 * mapScreen.js — 【骨架，尚未實作】「地圖」分頁畫面。
 * 觸控手勢（單指拖曳平移、單指輕點選取）與 canvas 繪製都在這裡，
 * 「打不打得贏」「能不能佔領」一律呼叫 Systems 層。
 * ==========================================================================*/

(function () {
  window.Game.UI.MapScreen = {
    // init(canvas) { ... }
    // render(saveGame, playerState) { ... }
  };
})();
