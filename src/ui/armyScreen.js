/* ============================================================================
 * armyScreen.js — 【骨架，尚未實作】「軍隊」分頁畫面（練兵、部隊列表、
 * 主將指派、拆分／解散）。
 * ==========================================================================*/

(function () {
  window.Game.UI.ArmyScreen = {
    // render(container, saveGame, playerState) { ... }
  };
})();
