/* ============================================================================
 * cityScreen.js — 【骨架，尚未實作】「城池」分頁畫面。
 * 只負責把 PlayerState／CityState 畫成 DOM 並綁定觸控事件；
 * 所有「能不能升級」「升級要花多少」之類的判斷都呼叫
 * window.Game.Systems.CityBuilding，不在畫面層重複寫規則。
 * ==========================================================================*/

(function () {
  window.Game.UI.CityScreen = {
    // render(container, saveGame, playerState) { ... }
  };
})();
