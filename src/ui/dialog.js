/* ============================================================================
 * dialog.js — 【骨架，尚未實作】觸控友善的自訂確認對話框／Toast 提示，
 * 取代 window.confirm（部分行動裝置 WebView 環境中原生對話框顯示不穩定）。
 * ==========================================================================*/

(function () {
  window.Game.UI.Dialog = {
    // showConfirm(message, okLabel, cancelLabel) { ... } // 回傳 Promise<boolean>
    // toast(message, ms) { ... }
  };
})();
