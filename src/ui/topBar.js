/* ============================================================================
 * topBar.js — 【骨架，尚未實作】頂部狀態列（資源、國力）。
 * ==========================================================================*/

(function () {
  window.Game.UI.TopBar = {
    // refresh(playerState) { ... }
  };
})();
