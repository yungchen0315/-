/* ============================================================================
 * bottomNav.js — 【骨架，尚未實作】底部分頁導覽（城池／地圖／武將／軍隊／戰役／戰報）。
 * ==========================================================================*/

(function () {
  window.Game.UI.BottomNav = {
    // init(onSwitchScreen) { ... }
    // setActive(screenId) { ... }
  };
})();
